import 'package:flutter/material.dart';

/*
 * Building the Billing 
 * --custom template for each pages
 * 
 */

class Billing extends StatelessWidget {
  // Variables


  // Constructor
  Billing();

  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}
